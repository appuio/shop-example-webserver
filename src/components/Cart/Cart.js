import React from "react";
import { Grid } from "semantic-ui-react";

const Cart = () => {
  return (
    <Grid.Row>
      <Grid.Column>
        cart
      </Grid.Column>
    </Grid.Row>
  );
};

export default Cart;
