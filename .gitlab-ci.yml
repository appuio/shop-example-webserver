stages:
    - test
    - compile
    - build

cache:
    key: "$CI_PROJECT_ID"
    paths:
        - .yarn
        - node_modules/

test:
    stage: test
    image: registry.vshn.net/roland.schlaefli/docs_runner_yarn2
    script:
        - yarn install --cache-folder=".yarn"
        # - yarn test
    artifacts:
        expire_in: "5min"
        paths:
            - ".yarn"

compile:
    stage: compile
    image: registry.vshn.net/roland.schlaefli/docs_runner_yarn2
    script:
        - yarn install --cache-folder=".yarn"
        - yarn build
    artifacts:
        expire_in: "5min"
        paths:
            - "build"
    only:
        - master

build:
    stage: build
    image: registry.vshn.net/roland.schlaefli/docs_runner_oc
    services:
        - docker:dind
    script:
        - oc login https://console.appuio.ch --token=$KUBE_TOKEN
        - docker login -u `oc whoami` -p `oc whoami -t` https://console.appuio.ch
        # - docker pull $OC_REGISTRY_IMAGE:latest
        # - docker build --cache-from $OC_REGISTRY_IMAGE:latest -t $OC_REGISTRY_IMAGE:latest .
        - docker build . -t $OC_REGISTRY_IMAGE:latest
        - docker push $OC_REGISTRY_IMAGE:latest
    only:
        - master
    tags:
        - dockerbuild
    variables:
        OC_REGISTRY_IMAGE: "https://console.appuio.ch/vshn-demoapp1/docs_example_webserver"